# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/fizz_connect

# Authentication Secrets (JWT)
JWT_SECRET=your_jwt_secret_key_here
